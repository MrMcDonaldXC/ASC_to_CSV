# ============================================================
# ASC to CSV 转换与可视化工具 - 依赖清单
# ============================================================
# Python版本要求: >= 3.8 (推荐 3.11)
# 安装命令: pip install -r requirements.txt
# ============================================================

# 核心依赖 - CAN总线工具
cantools==41.0.0

# 数据可视化
matplotlib==3.8.4
numpy==1.26.4

# 科学计算（可选，用于数据平滑）
scipy==1.13.0

# 打包工具（仅开发时需要）
pyinstaller==6.6.0

# 图标转换工具（仅开发时需要）
Pillow==10.3.0
