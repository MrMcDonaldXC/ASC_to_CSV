# ============================================================
# ASC to CSV 转换与可视化工具 - 依赖清单
# ============================================================
# Python版本要求: >= 3.8 (推荐 3.11)
# 安装命令: pip install -r requirements.txt
# ============================================================

# 核心依赖 - CAN总线工具
cantools>=37.0.0,<42.0.0

# 数据可视化
matplotlib>=3.5.0,<4.0.0
numpy>=1.21.0,<2.0.0

# 科学计算（可选，用于数据平滑）
scipy>=1.7.0,<2.0.0

# 打包工具（仅开发时需要）
pyinstaller>=5.0.0,<7.0.0
