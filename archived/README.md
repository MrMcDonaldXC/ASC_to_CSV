# 归档文件说明

本目录包含已被新版本替代的旧版本代码文件。

## 归档日期
2026-03-24

## 归档文件列表

### 源代码文件

| 文件 | 原用途 | 替代者 |
|------|--------|--------|
| `conversion_service.py` | 旧版转换服务 | `enhanced_conversion_service.py` |
| `data_processor.py` | 旧版数据处理器 | `enhanced_data_processor.py` |
| `csv_writer.py` | 旧版CSV写入器 | `enhanced_csv_writer.py` |

### 测试文件

| 文件 | 原用途 | 替代者 |
|------|--------|--------|
| `tests/test_conversion_service.py` | 旧版转换服务测试 | 需要重新编写 |

### 文档文件

| 文件 | 原用途 | 替代者 |
|------|--------|--------|
| `PlainTxT.txt` | 项目文档 | `docs/` 目录下的文档 |

## 归档原因

1. **代码重构**：新增了增强版模块，支持动态分组功能
2. **功能改进**：新模块支持更灵活的分组规则和更好的错误处理
3. **代码简化**：移除了不必要的策略选择，简化了用户界面

## 恢复说明

如需恢复这些文件：

1. 将文件从 `archived/` 目录移动回项目根目录
2. 更新相关导入语句
3. 更新 `main.py` 使用旧版模块

## 依赖关系

旧版模块依赖：
- `utils.py` 中的 `extract_batp_group()` 和 `sort_group_key()` 函数
- 这些函数在 `utils.py` 中保留，以支持旧版模块的潜在恢复需求
